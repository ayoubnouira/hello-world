# hello-world
This repository is for practicing the GitHub Flow.
# this is my first commit change
# this is my first issue fix
<hr>
<h2>about git:</h2>
<p id="e">git is a tool that help us:</p>
<ul>
  <li>save projects so they never get lost</li>
  <li>creating a clone of a prject than merge it into the main branch and commit it</li>
  <li>revert back to a previous version of a project</li>
  <li>to work with others easily</li>
</ul>
<p>git bash is my current terminal</p>
<h2>git command guide:</h2>
<ol>
  <li>these commands are used only once the first time installing git bash</li>
  <li><p>to clear the terminal</p></li>
  <li><p>i.its like telling the computer to where to look at</p><p>ii.to initialize a git folder(if i alr have a folder i MUST not do it again)</p></li>
</ol>
<h1 align=center>GIT COMMANDS</h1>
<ol>
  <li>Setup and configuration and help commands
    <ol>
      <li>git config --global user.name <strong>"Your Name"</strong></li>
      <li>git config --global user.email <strong>"you@example.com"</strong></li>
      <li>git config --global init.default branch main</li>
      <li>git config -h</li>
      <li>git help config
      <li>git help</li>
    </ol>
  </li>
  <li>clear the terminal
    <ol>
      <li>clear</li>
    </ol>
  </li>
  <li>change directory and create new git folder
    <ol>
      <li>cd <strong>c:/users/..rest of the file name</strong></li>
      <li>git init</li>
    </ol>
    
  </li>
</ol>

